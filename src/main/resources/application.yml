micronaut:
  application:
    name: stocks
  server:
    port: 8080
  router:
    static-resources:
      default:
        enabled: true
        mapping: "/public/**"
        paths: "classpath:public"
